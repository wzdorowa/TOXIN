include ../../elements/dropdown-row/dropdown-row.pug

mixin dropdown({label, placeholder = 'Выберите нужное количество', isHide = false, readonly = 'readonly', display = '', dropdownFor = ''} = {label, placeholder, isHide, readonly, display, dropdownFor})
  .dropdown
    .dropdown__list
      +base-input({
        label: label,
        placeholder: placeholder,
        arrow: 'arrow-down',
        hasBorderRadiusTop: true,
        isHide: isHide,
        readonly: readonly,
        areInDropdown: true,
      })
    .dropdown__content
      .dropdown__rows(class=`${'dropdown__rows_for-count-'}${dropdownFor}`)
        block
      .dropdown__buttons(class=`${'dropdown__buttons_'}${display}`)
        .dropdown__buttons-container.dropdown__buttons-container_for-clear
          +base-button({text: 'Очистить', decor: 'noDecor'})
        .dropdown__buttons-container.dropdown__buttons-container_for-apply
          +base-button({text: 'Применить', decor: 'noDecor'})
