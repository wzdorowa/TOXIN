mixin rating({ amountActiveStar = '5' } = {})
  -const amountStar = [1, 2, 3, 4, 5];
  ul.rating
    each value, index in amountStar
      if index < amountActiveStar 
        li.rating__item.rating__item_active
      else
        li.rating__item
